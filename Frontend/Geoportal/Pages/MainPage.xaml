<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:lang="clr-namespace:Geoportal.Resources.Languages"
             x:Class="GeoportalApp.Pages.MainPage"
             Shell.NavBarIsVisible="False">

    <Grid RowDefinitions="*, Auto">

        <ContentView x:Name="MainContent" Grid.Row="0" />

        <Border Grid.Row="1" 
                BackgroundColor="{AppThemeBinding Light=White, Dark=#1F1F1F}"
                StrokeShape="RoundRectangle 20,20,0,0"
                Padding="10,5"
                HeightRequest="70"
                StrokeThickness="0">
            <Border.Shadow>
                <Shadow Brush="Black" Offset="0,-5" Opacity="0.1" Radius="10" />
            </Border.Shadow>

            <Grid ColumnDefinitions="*,*,*,*">

                <VerticalStackLayout Grid.Column="0" VerticalOptions="Center">
                    <VerticalStackLayout.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnDashboardTapped" />
                    </VerticalStackLayout.GestureRecognizers>
                    <Label Text="📊" FontSize="24" HorizontalOptions="Center"/>
                    <Label Text="{x:Static lang:AppResources.TabDashboard}" FontSize="10" HorizontalOptions="Center"/>
                </VerticalStackLayout>

                <VerticalStackLayout Grid.Column="1" VerticalOptions="Center">
                    <VerticalStackLayout.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnReportTapped" />
                    </VerticalStackLayout.GestureRecognizers>
                    <Label Text="📝" FontSize="24" HorizontalOptions="Center"/>
                    <Label Text="{x:Static lang:AppResources.TabReport}" FontSize="10" HorizontalOptions="Center"/>
                </VerticalStackLayout>

                <VerticalStackLayout Grid.Column="2" VerticalOptions="Center">
                    <VerticalStackLayout.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnCompareTapped" />
                    </VerticalStackLayout.GestureRecognizers>
                    <Label Text="⚖️" FontSize="24" HorizontalOptions="Center"/>
                    <Label Text="{x:Static lang:AppResources.TabCompare}" FontSize="10" HorizontalOptions="Center"/>
                </VerticalStackLayout>

                <VerticalStackLayout Grid.Column="3" VerticalOptions="Center">
                    <VerticalStackLayout.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnSettingsTapped" />
                    </VerticalStackLayout.GestureRecognizers>
                    <Label Text="⚙️" FontSize="24" HorizontalOptions="Center"/>
                    <Label Text="{x:Static lang:AppResources.TabSettings}" FontSize="10" HorizontalOptions="Center"/>
                </VerticalStackLayout>

            </Grid>
        </Border>
    </Grid>
</ContentPage>