<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Geoportal.Pages.ReportsPage"
             Shell.NavBarIsVisible="False"
             BackgroundColor="Transparent">

    <Grid>
        <BoxView HorizontalOptions="Fill" VerticalOptions="Fill">
            <BoxView.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#0A56C4" Offset="0.1" />
                    <GradientStop Color="#3AB0C2" Offset="0.5" />
                    <GradientStop Color="#3FE543" Offset="1.0" />
                </LinearGradientBrush>
            </BoxView.Background>
        </BoxView>

        <ScrollView Padding="20,10,20,20">
            <VerticalStackLayout Spacing="15">

                <VerticalStackLayout Margin="0,20,0,10">
                    <Label Text="Мои отчеты" TextColor="White" FontSize="28" FontAttributes="Bold"/>
                    <Label Text="Список активных обращений" TextColor="White" FontSize="14" Opacity="0.8"/>
                </VerticalStackLayout>

                <Border StrokeShape="RoundRectangle 20" BackgroundColor="#CCFFFFFF" Padding="10">
                    <Grid ColumnDefinitions="*, *, *">
                        <Button Grid.Column="0" Text="Все" BackgroundColor="#0A56C4" TextColor="White" CornerRadius="10" HeightRequest="35" FontSize="11" Padding="5,0"/>
                        <Button Grid.Column="1" Text="В работе" BackgroundColor="Transparent" TextColor="#063A98" FontSize="11"/>
                        <Button Grid.Column="2" Text="Архив" BackgroundColor="Transparent" TextColor="#063A98" FontSize="11"/>
                    </Grid>
                </Border>

                <VerticalStackLayout x:Name="ReportsList" Spacing="12">

                    <Border StrokeShape="RoundRectangle 20" BackgroundColor="#CCFFFFFF" StrokeThickness="0" Padding="15">
                        <Grid ColumnDefinitions="50, *, Auto" RowDefinitions="Auto, Auto">
                            <Label Text="💡" FontSize="24" VerticalOptions="Center" HorizontalOptions="Center" Grid.RowSpan="2"/>

                            <VerticalStackLayout Grid.Column="1" Grid.Row="0" Margin="10,0">
                                <Label Text="Замена светильника" FontAttributes="Bold" TextColor="Black" FontSize="15"/>
                                <Label Text="№ 4451 - 24.02.2026" FontSize="11" TextColor="#6B7280"/>
                            </VerticalStackLayout>

                            <Label Grid.Column="2" Grid.Row="0" Text="В работе" TextColor="#0A56C4" FontSize="12" FontAttributes="Bold" VerticalOptions="Start"/>

                            <Label Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="1" Margin="10,5,0,0" 
                                   Text="Ул. Буюк Ипак Йули, замена опоры №12" FontSize="12" TextColor="#4B5563"/>
                        </Grid>
                    </Border>

                    <Border StrokeShape="RoundRectangle 20" BackgroundColor="#CCFFFFFF" StrokeThickness="0" Padding="15">
                        <Grid ColumnDefinitions="50, *, Auto" RowDefinitions="Auto, Auto">
                            <Label Text="🕳️" FontSize="24" VerticalOptions="Center" HorizontalOptions="Center" Grid.RowSpan="2"/>

                            <VerticalStackLayout Grid.Column="1" Grid.Row="0" Margin="10,0">
                                <Label Text="Яма на дороге" FontAttributes="Bold" TextColor="Black" FontSize="15"/>
                                <Label Text="№ 4448 - 23.02.2026" FontSize="11" TextColor="#6B7280"/>
                            </VerticalStackLayout>

                            <Label Grid.Column="2" Grid.Row="0" Text="Новый" TextColor="#16A34A" FontSize="12" FontAttributes="Bold" VerticalOptions="Start"/>

                            <Label Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="1" Margin="10,5,0,0" 
                                   Text="Перекресток Шахрисабз, требуется ямочный ремонт" FontSize="12" TextColor="#4B5563"/>
                        </Grid>
                    </Border>

                </VerticalStackLayout>

                <Button Text="+ Создать обращение" 
                        BackgroundColor="#16A34A" 
                        TextColor="White" 
                        FontAttributes="Bold"
                        HeightRequest="55"
                        CornerRadius="25"
                        Margin="0,10,0,0">
                    <Button.Shadow>
                        <Shadow Brush="Black" Offset="0,10" Opacity="0.3" Radius="20"/>
                    </Button.Shadow>
                </Button>

            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>